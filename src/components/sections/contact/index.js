import React from "react"
import styles from './styles.module.scss'

export default function Contact({ data: { title, text, image } }) {
  return (
    <section className={styles.wrapper}>
      <div>
        
      </div>
    </section>
  )
}